[Unit]
Description=Fomo Lens retention cleanup
[Service]
Type=oneshot
WorkingDirectory=/opt/fomo-lens
EnvironmentFile=/opt/fomo-lens/ops.env
ExecStart=/bin/sh -c 'RELEASE=$(cat .release) docker compose --profile ops run --rm ops npx tsx scripts/maintenance.ts'
User=fomo-lens-deploy
